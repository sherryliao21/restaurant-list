<head>
  <link rel="stylesheet" href="/stylesheets/edit.css">
</head>
<div class="banner"></div>
<h1 class="mb-1 restaurant-show-title">編輯餐廳</h1>
<div class="container d-flex justify-content-center">
  <form action="/restaurants/{{ restaurant._id }}?_method=PUT" method="POST">
    <div class="row d-flex justify-content-center">
      <div class="input-groups-left col-lg-5">
        <div class="input-groups">
          <label for="name">餐廳名稱*</label>
          <input autofocus required class="form-control mb-3" type="text" placeholder="name" name="name" class="input"
            value="{{ restaurant.name }}" maxlength="10">
        </div>
        <div class="input-groups">
          <label for="category">餐廳類別*</label>
          <input required class="form-control mb-3" type="text" placeholder="category" name="category" class="input"
            value="{{ restaurant.category }}" maxlength="10">
        </div>
        <div class="input-groups">
          <label for="image">圖片連結*</label>
          <input required class="form-control mb-3" type="text" placeholder="image" name="image" class="input"
            value="{{ restaurant.image }}">
        </div>
        <div class="input-groups">
          <label for="location">餐廳地址*</label>
          <input required class="form-control" type="text" placeholder="location" name="location" class="input"
            value="{{ restaurant.location }}">
        </div>
      </div>
      <div class="input-groups-right col-lg-5">
        <div class="input-groups">
          <label for="phone">電話號碼*</label>
          <input required class="form-control mb-3" type="tel" pattern="[0]{1}[0-9]{9}" placeholder="phone" name="phone"
            class="input" value="{{ restaurant.phone }}">
        </div>
        <div class="input-groups">
          <label for="rating">餐廳評分*</label>
          <input class="form-control mb-3" type="number" min="1.0" max="5.0" step="0.1" placeholder="rating"
            name="rating" class="input" value="{{ restaurant.rating }}">
        </div>
        <div class="input-groups">
          <label for="description">餐廳概述*</label>
          <textarea required rows="4" class="form-control" type="text" placeholder="description" name="description"
            class="input" maxlength="50">{{ restaurant.description }}</textarea>
        </div>
      </div>
    </div>
    <div class="button d-flex justify-content-center mt-3">
      <button type="submit" class="btn btn-save">儲存</button>
      <button class="btn btn-back">
        <a href="/" style="text-decoration: none; color: #fff;">返回</a>
      </button>
    </div>
  </form>
</div>